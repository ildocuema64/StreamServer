<!--
  Icecast config for local dev (backend on host, no Docker).
  Auth: mount-level stream_auth → backend validates per-station passwords.
-->
<icecast>
    <limits>
        <clients>1000</clients>
        <sources>10</sources>
        <queue-size>524288</queue-size>
        <client-timeout>30</client-timeout>
        <header-timeout>15</header-timeout>
        <source-timeout>10</source-timeout>
        <burst-on-connect>1</burst-on-connect>
        <burst-size>65535</burst-size>
    </limits>

    <authentication>
        <relay-password>relaypass</relay-password>
        <admin-user>admin</admin-user>
        <admin-password>adminpass</admin-password>
    </authentication>

    <hostname>localhost</hostname>
    <location>StreamServer Radio</location>
    <admin>admin@streamserver.local</admin>

    <fileserve>1</fileserve>
    <paths>
        <basedir>/usr/local/share/icecast</basedir>
        <logdir>/tmp/icecast-logs</logdir>
        <webroot>/usr/local/share/icecast/web</webroot>
        <adminroot>/usr/local/share/icecast/admin</adminroot>
        <alias source="/" destination="/status.xsl"/>
    </paths>

    <http-headers>
        <header name="Access-Control-Allow-Origin" value="*" />
        <header name="Access-Control-Allow-Headers" value="Origin, Accept, X-Requested-With, Content-Type, Icy-MetaData" />
        <header name="Access-Control-Allow-Methods" value="GET, OPTIONS, HEAD" />
    </http-headers>

    <listen-socket>
        <port>8000</port>
        <bind-address>0.0.0.0</bind-address>
    </listen-socket>

    <!-- Default mount template: URL auth for ALL dynamic mounts (per-station passwords) -->
    <mount type="default">
        <public>1</public>
        <max-listeners>500</max-listeners>
        <authentication type="url">
            <option name="stream_auth" value="http://127.0.0.1:3000/api/internal/icecast/auth"/>
            <option name="auth_header" value="icecast-auth-user: 1"/>
        </authentication>
    </mount>

    <mount type="normal">
        <mount-name>/live</mount-name>
        <stream-name>StreamServer Live</stream-name>
        <stream-description>Live Broadcasting</stream-description>
        <genre>Various</genre>
        <bitrate>128</bitrate>
        <type>audio/mpeg</type>
        <authentication type="url">
            <option name="stream_auth" value="http://127.0.0.1:3000/api/internal/icecast/auth"/>
            <option name="auth_header" value="icecast-auth-user: 1"/>
        </authentication>
    </mount>

    <logging>
        <accesslog>access.log</accesslog>
        <errorlog>error.log</errorlog>
        <loglevel>3</loglevel>
        <logsize>10000</logsize>
    </logging>

    <security>
        <chroot>0</chroot>
    </security>
</icecast>
