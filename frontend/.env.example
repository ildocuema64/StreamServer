# Copiar para frontend/.env – usado pelo Vite em dev

# Local: proxy Vite para o backend (pode omitir se usares o default 127.0.0.1:3000)
# VITE_BACKEND_URL=http://127.0.0.1:3000

# Vercel — editar a variável EXISTENTE (não criar duplicada):
#   VITE_BACKEND_URL=https://NOME-DO-SERVICO.onrender.com
# NÃO uses 127.0.0.1 nem YOUR_API_DOMAIN na Vercel.
#
# Render (backend) — variáveis obrigatórias para URLs públicas:
#   APP_URL=https://o-teu-app.vercel.app
#   PUBLIC_ICECAST_HOST=host-publico-do-icecast   (BUTT transmite para aqui)
#   ICECAST_HOST=host-publico-do-icecast          (proxy /stream no backend)
#   ICECAST_DISABLED=false

# Multicaixa Express — número mostrado no modal de pagamento
VITE_PAYMENT_EXPRESS_PHONE=921923232

# Supabase no browser (opcional – @supabase/supabase-js)
VITE_SUPABASE_URL=https://YOUR_REF.supabase.co
VITE_SUPABASE_ANON_KEY=
